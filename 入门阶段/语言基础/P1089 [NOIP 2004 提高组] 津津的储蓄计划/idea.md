**预算不够的情况：**
```c++
n=n+300-y; // 加上300再减去预算
if(n<0) {
    cout<<-i<<endl;
    return 0;
}
```
直接这样判断并输出-{月份}即可

**判断还能剩下多少钱**
```c++
while(n>=100) {
    n=n-100; // 减到n<100
    s=s+100; // 剩余的钱+100
}
```

